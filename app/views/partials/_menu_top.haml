#menu-top
  .contain-to-grid
    .fixed
      %nav.top-bar

        -# == Title Area =======================================================

        %ul.title-area
          %li.name
            %h1= link_to @kb.ask(:website_info)[:acronym], root_path
          %li.toggle-topbar.menu-icon
            %a{ :href => '#' }
              %span Menu

        -# == Navigation =======================================================

        %section.top-bar-section

          -# == Left Navigation ================================================

          %ul.left
            %li asd

          -# == Right Navigation ===============================================

          %ul.right

            %li
              = link_to 'About', 'javascript:void(0)', :class => 'social foundicon-torso go-to-bottom trigger-click', :'data-trigger-click-target' => '#about-project-title'
